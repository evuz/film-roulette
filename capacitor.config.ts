import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dhormigo.filmroulette',
  appName: 'Film roulette',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
